spring:

    datasource:
      url: jdbc:postgresql://localhost:5432/spring_boot_course_24
      username: postgres
      password: Janina1.
      driver-class-name: org.postgresql.Driver
      validation-query: SELECT 1
      test-on-borrow: true

    jpa:
      database-platform: org.hibernate.dialect.PostgreSQLDialect
      properties:
        hibernate.boot.allow_jdbc_metadata_access: false
      hibernate:
        ddl-auto: none
      sql:
        init:
          mode: never

    mvc:
      dispatch-options-request: true


     # base configuration of security
    security:
     user:
      name: vani
      password: 123

# Optionally, if you want to configure the schema management behavior:
# spring.jpa:
#   hibernate:
#     ddl-auto: update   # For automatic schema updates (use with caution)
#   show-sql: true       # Shows the SQL queries generated by Hibernate

management:
  endpoints:
    web:
      exposure:
        include: beans,loggers,metrics,health,info,readiness

  # disable
  endpoint:
    quartz:
      enabled: false
    loggers:
      enabled: false
    health:
      show-details: always




server:
  port: 8081